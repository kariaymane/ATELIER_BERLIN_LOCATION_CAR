# Set the API origin, without /api/v1. Use HTTPS outside local development.
API_BASE_URL=http://127.0.0.1:8000
DEFAULT_LANGUAGE=fr
DEFAULT_THEME=emerald
# Optional per-user state directory; leave unset for the platform default.
# CAR_RENTAL_DATA_DIR=
