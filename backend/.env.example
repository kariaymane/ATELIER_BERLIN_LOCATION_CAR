# Native backend development only. Docker uses docker/.env instead.
# Provide PostgreSQL URLs for a disposable local database, never production.
DATABASE_URL=
DATABASE_URL_SYNC=
JWT_SECRET=
JWT_REFRESH_SECRET=
CORS_ORIGINS=
DEBUG=false
ADMIN_EMAIL=
ADMIN_PASSWORD=
ADMIN_FULLNAME=Administrator
