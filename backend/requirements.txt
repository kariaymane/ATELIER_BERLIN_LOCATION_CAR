# Runtime dependencies for the deployed API.
# Anything only the test suite needs belongs in requirements-dev.txt — it must
# not reach the production image, where every package is extra attack surface.

fastapi==0.115.12
uvicorn[standard]==0.34.3
sqlalchemy[asyncio]==2.0.41
asyncpg==0.30.0
alembic==1.16.1
pydantic==2.11.3
pydantic-settings==2.9.1
python-jose[cryptography]==3.4.0
argon2-cffi==23.1.0
python-multipart==0.0.20
slowapi==0.1.9

# Alembic runs `alembic upgrade head` as the Fly release command over the SYNC
# DSN (postgresql://), which needs a synchronous driver.
psycopg2-binary>=2.9,<3
